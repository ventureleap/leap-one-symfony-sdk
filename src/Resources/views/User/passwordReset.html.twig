{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <div class="col-md-6 mx-auto">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title text-center py-3 font-weight-bold">
                        {{ 'label.resetPassword' | trans }}
                    </h4>
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
