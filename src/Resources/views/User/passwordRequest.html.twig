{% extends 'base.html.twig' %}

{% block content %}
    <h1>{{ 'message.provideEmail' | trans }}</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}
    {{ form_end(form) }}
{% endblock %}
